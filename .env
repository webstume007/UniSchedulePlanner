# ENVIRONMENT VARIABLES
# Keep this file secret and never upload it to GitHub.

# Database URL (Defaulting to local SQLite for development)
DATABASE_URL="sqlite:///timetable.db"

# Streamlit Configuration (Optional limits)
MAX_UPLOAD_SIZE=10
